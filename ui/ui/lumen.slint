import { ViewStack, IslandMode } from "views/ViewStack.slint";
export component LumenOverlay inherits Window {
    in property <IslandMode> mode;

    always-on-top: true;

    ViewStack {
        mode: root.mode;
    }
}