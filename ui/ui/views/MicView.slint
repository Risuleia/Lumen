export component MicView {
    in property <float> level;

    Rectangle {
        background: transparent;

        Text {
            text: "Listening…";
            color: white;
            x: 12px;
            y: 12px;
        }
    }
}